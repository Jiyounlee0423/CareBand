<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.CareBand" parent="android:Theme.Material.Light.NoActionBar" />
    <style name="Theme.CareBand.Splash" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@color/white</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <!-- ✅ 팝업용 다이얼로그 테마 추가 -->
    <style name="Theme.App.Dialog" parent="Theme.Material3.DayNight.Dialog">
        <!-- 배경 흐리게 + 다이얼로그 스타일 -->
        <item name="android:windowIsFloating">true</item>
        <item name="android:backgroundDimEnabled">true</item>
    </style>

</resources>